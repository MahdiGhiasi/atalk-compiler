# Code #17
# Syntax Analysis: OK
# Semantic Analysis: OK
# Output: [Nothing]

actor Math<10>
	receiver calcSumUpToZero(int[10] array)
		int sum = 0
		foreach element in array
		    int mahdi = 3
			if element <> 0
			    char mehrdad = 'a'
				sum = sum + element
			else
				break
			end
		end
		int farbod = 2
		sender << calcSumUpToZeroCompleted(sum)
	end
end